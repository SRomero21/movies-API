const express = requiere('express')

const {} = require()

const router = express.Router();

router.get('/', )

router.post('/')

router.patch('/')

router.delete('/')

module.exports = { actorsRouter: router}